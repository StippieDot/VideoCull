---
title: Review mode
description: Step through videos one at a time with playback, bookmarks, metadata, and keyboard-first keep or delete decisions.
---

{/* in-app-meta
{
  "summary": "Use review mode when keyboard-first decisions are faster than browsing the grid.",
  "actions": ["show-shortcuts", "open-settings-keybindings"]
}
*/}

Review mode is the one-at-a-time workflow for clips that need a closer look. It combines a full thumbnail strip, video metadata, and an in-app player with keyboard-first controls.

![Review mode](/screenshots/V2.0.0%20-%20ReviewMode.png)

## When review mode helps

Review mode turns the current scope into a one-by-one decision queue with playback, navigation, and undo built into the same flow.

Enter review mode after the grid has already narrowed the library to the queue you actually want to judge clip by clip.

## Decision controls

Keep and Delete are final review outcomes. Skip means intentionally undecided. Reset returns a clip to Pending. Undo reverses the most recent change without losing your place.

Use Keep or Delete for clear decisions, Skip when you need a second pass, and Undo instead of hunting backward after a fast mistake.

## Scope and playback

Review progress is calculated from the active scope, which can be the full session, the current filter, one folder, or one duplicate group.

Check the scope label before you start. Use built-in playback for the normal path and open the external player only when the embedded player is not reliable enough for that clip.

## Core controls

- Press `K` to keep the current video.
- Press `D` to mark it for deletion.
- Press `S` to skip it.
- Press `Z` to undo the last decision.
- Press `Enter` to play in-app or `Ctrl+Enter` to open the file in your external player.

After a decision, Video Cull can move straight to the next clip so you can stay in flow.

The in-app [Keyboard shortcuts](/reference/keyboard-shortcuts) page shows your configured bindings, so use that reference if you have changed the defaults listed here.

## Playback and navigation

- Scrub through the video inside the app.
- Change playback speed with `[` and `]`.
- Use `Left` and `Right` to navigate, or scrub 5 seconds while a video is playing.
- Jump to the next undecided video instead of revisiting clips you already handled.

## Metadata and bookmarks

Review mode also keeps the current video's context close at hand:

- Ratings and favorites can be changed directly from the review screen.
- Bookmarks can be dropped while the video is playing and revisited later.
- Layout adapts to normal, maximized, and high-resolution displays.

<Note>
  If the built-in player should not handle a file, use the external player handoff instead of forcing playback in-app.
</Note>
